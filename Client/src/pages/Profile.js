import React, { useState } from "react";
import axios from "axios";

const Profile = () => {

    // Return JSX
    return(
        <main>
            <p>Profile Page</p>
        </main>
    );
}

export default Profile;
