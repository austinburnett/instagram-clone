import React, { useState } from "react";
import axios from "axios";

const Profile = () => {

    // Return JSX
    return(
        <main>
            <p>Home Page</p>
        </main>
    );
}

export default Profile;
